#pragma once

#include <iostream>
#include <limits>
#include <string>
#include <vector>

using namespace std;

template<typename T>
T standardIn(string tips);
vector<string> split(string str, string separator);